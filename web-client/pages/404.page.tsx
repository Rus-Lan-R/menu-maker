import Default from "@/layouts/Default";
import Text from "@/modules/Text/Text";

export default function Page404() {
  return (
    <Default>
      <Text type="text1" color="var(--font-color-gray)">
        Something went wrong, but it`s never too late to fix it
      </Text>
    </Default>
  );
}
